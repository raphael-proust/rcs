[user]
  name = Raphaël Proust
  email = raphlalou@gmail.com

[diff]
  tool = fugitive

[difftool "fugitive"]
  cmd = "vim -f $REMOTE -c 'Gdiff'"

[merge]
  tool = splice

[mergetool "splice"]
  cmd = "vim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
  trustExitCode = true

[alias]
  co = commit
  st = "!f() { echo $(git status | head -1) && echo $(git status | grep 'Your branch' | head -n 1) && git status -s; }; f"
