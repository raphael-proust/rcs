#!/bin/dash

case "$1" in
  toggle|on|off)
    amixer set Master "$1" >/dev/null
    ;;

  up)
    amixer set Master playback 3+ >/dev/null
    ;;

  down)
    amixer set Master playback 3- >/dev/null
    ;;
esac

set-status-line-bnwr

